"use client";
import { useState } from "react";
import { AppShell } from "../../components/app-shell";
const channels = [{ name: "WhatsApp Business", copy: "Connect a verified business account", connected: false }, { name: "Instagram", copy: "Manage business DMs and comments", connected: false }, { name: "Email", copy: "Send and receive from your business domain", connected: false }, { name: "OpenAI", copy: "AI reply drafts are ready on your server", connected: true }];
export default function SettingsPage() { const [items, setItems] = useState(channels); return <AppShell title="Settings" subtitle="Manage your workspace, channels, and AI controls."><section className="settings-section"><h2>Connected channels</h2><p>Only connect accounts that your business owns and has permission to use.</p><div className="integration-list">{items.map((item, index) => <article key={item.name}><span>{item.name[0]}</span><div><h3>{item.name}</h3><p>{item.copy}</p></div><button className={item.connected ? "connected" : "connect"} onClick={() => setItems(items.map((entry, entryIndex) => entryIndex === index ? { ...entry, connected: !entry.connected } : entry))}>{item.connected ? "Connected" : "Connect"}</button></article>)}</div></section><section className="settings-section"><h2>AI autonomy</h2><p>Draft mode keeps people in control while you set up the workspace.</p><div className="autonomy"><strong>Draft only</strong><span>AI can analyse conversations and create replies. A human must approve every outbound message.</span><button className="selected-mode">Current mode</button></div></section></AppShell>; }
